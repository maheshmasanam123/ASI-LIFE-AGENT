name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: '24'

jobs:
  typecheck:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript type check
        run: npm run typecheck

  lint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

  build:
    name: Next.js Build
    runs-on: ubuntu-latest
    needs: [typecheck, lint]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js application
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1

  smoke-test:
    name: Smoke Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start WebSocket server
        run: |
          npx tsx agents/websocket-server.ts > /tmp/ws.log 2>&1 &
          echo $! > /tmp/ws.pid
          sleep 5

      - name: Start Next.js server
        run: |
          npm run dev > /tmp/next.log 2>&1 &
          echo $! > /tmp/next.pid
          sleep 15

      - name: Wait for services
        run: |
          for i in {1..30}; do
            if curl -sf http://localhost:3001/health >/dev/null 2>&1; then
              echo "WebSocket server ready"
              break
            fi
            sleep 1
          done
          for i in {1..30}; do
            if curl -sf http://localhost:3000/api/health >/dev/null 2>&1; then
              echo "Next.js server ready"
              break
            fi
            sleep 1
          done

      - name: Run smoke test
        run: |
          curl -f http://localhost:3001/health || exit 1
          curl -f http://localhost:3000/api/health || exit 1
          curl -X POST http://localhost:3000/api/tools/execute \
            -H "Content-Type: application/json" \
            -d '{"tool":"file","input":{"operation":"read","path":"/etc/hostname"}}' | grep -q "success" || exit 1

      - name: Cleanup
        if: always()
        run: |
          kill $(cat /tmp/ws.pid 2>/dev/null) 2>/dev/null || true
          kill $(cat /tmp/next.pid 2>/dev/null) 2>/dev/null || true

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --if-present

  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    needs: [build, smoke-test, test]
    if: github.event_name == 'pull_request'
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - name: Deploy to Vercel Preview
        if: env.VERCEL_TOKEN != ''
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod=false'